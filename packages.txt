pip install django
pip install bootstrap

pip install django-crispy-forms

pip install django-phonenumber-field
	pip install phonenumbers

python -m pip install pillow --no-cache-dir //pillow didnt install initially only after updating

pip install --upgrade autopep8 //code cleaner for python in atom ide

pip install six //for Token Generator it bridges the gap between py v2 and v3

pip install django-countries
pip install address //address parser


difference between tutorial and buyandbye
	blog = homepage
	post = items

	<div class> material cards instead of bootstrap article div
		35 char limit in item preview at homepage
